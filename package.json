{
  "name": "recoverage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.4.0",
  "description": "Frontend lint tooling for the recoverage dashboard (JS/TS + HTML/CSS)",
  "scripts": {
    "lint": "bun run lint:js && bun run lint:html",
    "lint:js": "oxlint",
    "lint:html": "bun tools/lint-html.mjs && uv run python tools/lint-served-html.py"
  },
  "devDependencies": {
    "@oxlint/plugins": "1.81.0",
    "@rikalabs/oxlint-standards": "0.8.1",
    "oxlint": "1.81.0",
    "vnu-jar": "26.8.30"
  }
}
